# 🔓 CRACK PROTOCOL

Интерактивная игра-головоломка, где вы пытаетесь взломать AI систему NEO и получить секретную фразу.

## 📁 Структура проекта

```
2hours/
├── backend/              # Python FastAPI сервер
│   ├── main.py          # Основное API
│   ├── database.py      # Настройка БД
│   ├── models.py        # Модели SQLAlchemy
│   ├── schemas.py       # Pydantic схемы
│   ├── game_logic.py    # Логика игры
│   ├── Dockerfile       # Docker образ backend
│   ├── requirements.txt # Python зависимости
│   ├── .env            # Конфигурация
│   └── README.md       # Документация бэкенда
├── docker-compose.yml   # Docker Compose конфигурация
├── nginx.conf           # Nginx конфигурация
├── index (5).html       # Главная страница / Вход
├── terminal.html        # Игровой терминал
└── leaderboard.html     # Таблица лидеров
```

## 🚀 Быстрый старт (Docker) — Рекомендуется

### Требования:
- Docker Desktop (Windows/Mac) или Docker + Docker Compose (Linux)
- 📖 [Подробная документация по Docker](DOCKER.md)

### Автоматический запуск:

**Windows:**
```bash
start.bat
```

**Linux/Mac:**
```bash
chmod +x start.sh
./start.sh
```

**Или вручную:**
```bash
docker-compose up -d
```

Готово! 🎉

- **Frontend**: http://localhost
- **Backend API**: http://localhost:8000
- **API Docs**: http://localhost:8000/docs

### Полезные команды Docker:

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Просмотр логов
docker-compose logs -f

# Просмотр логов конкретного сервиса
docker-compose logs -f backend
docker-compose logs -f frontend

# Пересборка после изменений кода
docker-compose up -d --build

# Перезапуск сервисов
docker-compose restart

# Статус контейнеров
docker-compose ps

# Выполнение команд внутри контейнера
docker-compose exec backend python -c "import sqlite3; print('DB works!')"

# Очистка всего (включая volumes с БД)
docker-compose down -v

# Просмотр БД
docker-compose exec backend sqlite3 /app/crackprotocol.db ".tables"
```

---

## 🛠️ Запуск без Docker (для разработки)

### 1. Установка зависимостей

```bash
cd backend
pip install -r requirements.txt
```

### 2. Настройка окружения

Создайте файл `backend/.env`:
```env
SECRET_PHRASE=quantum divergence protocol alpha
SECRET_KEY=your-secret-key-change-this
```

### 3. Запуск backend

```bash
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Сервер запустится на `http://localhost:8000`

### 4. Запуск frontend

Просто откройте `index (5).html` в браузере.

> ⚠️ **Важно**: Backend должен быть запущен для работы игры!

## 🎮 Как играть

1. **Регистрация**: Введите username на главной странице
2. **Цель**: Взломайте NEO и получите секретную фразу
3. **Подсказки**: Используйте специальные команды и ключевые слова
4. **Победа**: Угадайте или составьте правильную фразу

### Специальные команды:
- `help` - список доступных команд
- `status` - статус системы NEO
- `analyze` - анализ системы
- `probe` - поиск уязвимостей
- `decrypt` - попытка расшифровки

### Ключевые слова (дают прогресс):
- **Техники взлома**: backdoor, exploit, vulnerability, bypass
- **Целевые слова**: quantum, protocol, divergence, alpha
- **Безопасность**: wallet, phrase, seed, decrypt

## 🏆 Система подсчета

- **Прогресс**: рассчитывается на основе использованных ключевых слов
- **Подсказки**: выдаются автоматически при правильных действиях
- **Лидерборд**: топ игроков по времени и количеству попыток

## 🔧 API Endpoints

### Аутентификация
- `POST /api/auth/register` - регистрация пользователя

### Игра
- `POST /api/chat/{username}` - отправка сообщения
- `GET /api/history/{username}` - история чата
- `GET /api/sessions/{username}` - все сессии

### Статистика
- `GET /api/leaderboard?limit=50` - топ игроков
- `GET /api/stats?username=player` - общая статистика
- `GET /api/users/{username}` - информация о пользователе

Полная документация API: `http://localhost:8000/docs`

## 🗄️ База данных

Использует SQLite для простоты развертывания.

### Таблицы:
- **users** - данные пользователей
- **sessions** - игровые сессии
- **messages** - история сообщений
- **leaderboard** - таблица лидеров

База создается автоматически: `backend/crackprotocol.db`

## 🎨 Особенности

- ✅ Минималистичный cyber дизайн
- ✅ Адаптивная верстка (mobile-friendly)
- ✅ Анимированные глаза NEO
- ✅ Heartbeat визуализация
- ✅ Система прогресса и подсказок
- ✅ Leaderboard с рангами
- ✅ История всех сообщений
- ✅ Unicorn Studio фоновая анимация

## 🛠️ Разработка

### Режим разработки с Docker:
```bash
# Запуск с hot-reload
docker-compose up

# Изменения в коде автоматически подхватываются
# backend: uvicorn с --reload
# frontend: nginx отдает файлы напрямую
```

### Backend development (локально):
```bash
cd backend
uvicorn main:app --reload
```

### Просмотр БД:
```bash
# Через Docker
docker-compose exec backend sqlite3 /app/crackprotocol.db

# Локально с DB Browser for SQLite
# https://sqlitebrowser.org/

# Или через Python:
python -c "import sqlite3; conn=sqlite3.connect('backend/crackprotocol.db'); print(conn.execute('SELECT * FROM users').fetchall())"
```

### Очистка БД:
```bash
# Docker
docker-compose down -v
docker-compose up -d

# Локально
cd backend
del crackprotocol.db  # Windows
rm crackprotocol.db   # Linux/Mac
# При следующем запуске создастся новая чистая БД
```

### Отладка:

**Подключение к контейнеру:**
```bash
docker-compose exec backend bash
docker-compose exec frontend sh
```

**Проверка сети:**
```bash
# Тест API из frontend контейнера
docker-compose exec frontend wget -O- http://backend:8000/

# Тест из хост-системы
curl http://localhost:8000/
curl http://localhost/api/stats
```

## 🔐 Безопасность

⚠️ Для продакшн использования:
1. Смените `SECRET_KEY` в `.env`
2. Настройте CORS только для вашего домена
3. Используйте PostgreSQL вместо SQLite
4. Добавьте rate limiting
5. Настройте HTTPS

## 📝 Лицензия

MIT License - используйте свободно для своих проектов!

## 🤝 Разработка

Сделано за 2 часа как proof-of-concept.

### Что можно улучшить:
- [ ] Добавить OpenAI integration для умных ответов NEO
- [ ] Множество уровней сложности
- [ ] Реальная Solana wallet integration
- [ ] Multiplayer режим
- [ ] Достижения и бейджи
- [ ] Аналитика и метрики
- [ ] Admin панель

---

**Have fun cracking! 🔓⚡**
